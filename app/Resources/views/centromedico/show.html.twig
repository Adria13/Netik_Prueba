{% extends 'base.html.twig' %}

{% block body %}
    <h1>Centromedico</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ centroMedico.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ centroMedico.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ centroMedico.email }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('centromedico_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('centromedico_edit', { 'id': centroMedico.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
